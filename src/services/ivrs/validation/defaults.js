// iv_timeout will be 10 if user does not specify as a result of choosing a timeout destination
// iv_maxloops = 2 if not specified from by user based on above
// iv_me_id will be 0 if a media file is not selected.

module.exports = {
  allowFeatures: '',
  digitTimeout: 2,
  allowCustom: '',
  loopPlayMsgOnce: '',
};
